// Forward-declaration для cycle overlay v0.2.
// В Phase 0.6 используется только как prop type на CalendarMonthDayCell —
// рендер фактический добавится когда v0.2 cycle-фича актуальна.

export type CyclePhaseToken = "period" | "fertile" | "ovulation" | "none";
